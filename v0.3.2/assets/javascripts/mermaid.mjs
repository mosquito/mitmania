// Keep MkDocs builds offline-safe; the browser resolves the pinned Mermaid
// module when a page containing a diagram is viewed.
export { default } from "https://unpkg.com/mermaid@10.4.0/dist/mermaid.esm.min.mjs";
